# Linux initialisation script

Script to initialise a basic linux environment (currently) based on Debian 12.

## Install

### Debian

Start off with a basic install of Debian, sans desktop environment.

As root run:
`./initdebian.sh`
